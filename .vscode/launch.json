{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (Linux)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/knowledgebase",
			"cwd": "${workspaceFolder}/server",
            // "environment": [{ "name": "config", "value": "Debug" }],
            "args": []
        }
    ]
}